# YouTube Shorts 管理系統

現代化的 YouTube Shorts 自動化管理系統，使用 Vue 3 + FastAPI 打造。

## 🎯 功能特色

### 前端功能
- 📊 **儀表板** - 總覽系統狀態和統計數據
- 📹 **影片管理** - 瀏覽、篩選、編輯所有影片
- 🔍 **爆款發現** - 自動發現熱門 Shorts 或手動添加
- 🤖 **AI 分析** - 使用 Gemini AI 分析影片內容
- 📅 **排程管理** - 智能排程影片發布時間
- ⚙️ **系統設定** - 配置 API 和系統參數

### 後端功能
- FastAPI REST API
- SQLAlchemy ORM
- YouTube Data API 整合
- Gemini AI 整合
- 自動化排程系統

## 🚀 快速開始

### 前置需求
- Python 3.12+
- Node.js 18+
- YouTube API 金鑰
- Gemini API 金鑰

### 安裝步驟

1. **安裝 Python 依賴**
```bash
pip install -r requirements.txt
# 或使用 uv
uv pip install -r requirements.txt
```

2. **安裝前端依賴**
```bash
cd web
npm install
```

3. **配置環境變數**
建立 `.env` 文件並設定：
```env
YOUTUBE_API_KEY=your_youtube_api_key
GEMINI_API_KEY=your_gemini_api_key
```

4. **初始化資料庫**
```bash
alembic upgrade head
```

### 開發模式

1. **啟動後端 API**
```bash
# 在專案根目錄
python src/ui/api_server.py
# API 將運行在 http://localhost:8000
```

2. **啟動前端開發伺服器**
```bash
cd web
npm run dev
# 前端將運行在 http://localhost:3000
```

3. **訪問系統**
開啟瀏覽器訪問 http://localhost:3000

### 生產部署

1. **建置前端**
```bash
cd web
npm run build
```

2. **啟動生產伺服器**
```bash
uvicorn src.ui.api_server:app --host 0.0.0.0 --port 8000
```

## 📁 專案結構

```
youtube-short/
├── web/                    # Vue 3 前端
│   ├── src/
│   │   ├── views/         # 頁面組件
│   │   ├── components/    # 可重用組件
│   │   ├── stores/        # Pinia 狀態管理
│   │   ├── router/        # Vue Router
│   │   ├── api/           # API 客戶端
│   │   └── assets/        # 靜態資源
│   ├── index.html
│   └── vite.config.js
│
├── src/                    # Python 後端
│   ├── ui/                # Web UI 和 API
│   │   └── api_server.py  # FastAPI 伺服器
│   ├── discovery/         # 爆款發現
│   ├── analysis/          # AI 分析
│   ├── publishing/        # 發布管理
│   ├── storage/           # 資料存儲
│   └── utils/             # 工具函數
│
├── config/                # 配置文件
├── scripts/               # 執行腳本
└── docs/                  # 文檔
```

## 🎨 技術棧

### 前端
- **Vue 3** - 漸進式 JavaScript 框架
- **Vue Router** - 官方路由管理
- **Pinia** - 狀態管理
- **Axios** - HTTP 客戶端
- **Vite** - 快速建置工具

### 後端
- **FastAPI** - 現代 Python Web 框架
- **SQLAlchemy** - ORM
- **Alembic** - 資料庫遷移
- **Google API Client** - YouTube API
- **Gemini AI** - 影片分析

## 📖 使用指南

### 1. 發現爆款影片
- 進入「爆款發現」頁面
- 設定最小觀看數和數量
- 點擊「開始發現」自動抓取熱門影片

### 2. 管理影片
- 在「影片管理」頁面查看所有影片
- 使用篩選功能快速找到目標影片
- 點擊操作按鈕進行分析、編輯或刪除

### 3. AI 分析
- 進入「AI 分析」頁面
- 選擇要分析的影片或批量分析
- 查看 AI 生成的建議和評分

### 4. 排程發布
- 在「排程管理」頁面新增排程
- 選擇影片、平台和發布時間
- 系統將自動在指定時間發布

## 🔧 API 文檔

啟動後端後訪問 http://localhost:8000/docs 查看完整的 API 文檔（Swagger UI）。

主要 API 端點：
- `GET /api/health` - 健康檢查
- `GET /api/stats` - 統計數據
- `GET /api/videos` - 影片列表
- `POST /api/discover/viral` - 發現爆款
- `POST /api/analysis/video/{id}` - 分析影片
- `POST /api/schedule` - 新增排程

## 🤝 貢獻

歡迎提交 Issue 和 Pull Request！

## 📄 授權

ISC License
